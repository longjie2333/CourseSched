# 项目说明

CourseSched 是“小江课表”原生微信小程序，提供课表、考试时间、学期报告和订阅日历等功能。

- `pages/`：页面入口与页面交互逻辑。
- `components/`：可复用微信小程序组件；组件通过 `*.js`、`*.wxml`、`*.wxss`、`*.json` 配套维护。
- `modules/`：按业务域组织的状态和服务层，目前包括认证、课表、公告和学期报告。
- `utils/`、`constants/`：请求、错误处理、持久化、日志和通用工具。
- `miniprogram_npm/tdesign-miniprogram/`：TDesign 小程序组件；优先使用现有依赖，不重复实现通用组件。

项目使用 MobX 管理跨页面业务状态，使用 TDesign Miniprogram 提供基础 UI。页面负责展示和交互，业务数据与请求状态集中在对应 `modules/`，接口请求统一经过 `utils/request.js`。

# 编码风格

- 使用 ES Module、单引号和无分号风格；缩进、换行和命名优先遵循所在文件及相邻代码的既有写法。
- 保持页面、组件、业务模块和工具职责分离；新增逻辑放在最接近其职责的目录，不在页面中复制业务规则。
- 优先复用项目已有依赖、Store、Service 和工具函数；不为废弃路径增加兼容层，不做未经需求支持的抽象或配置。
- 组件使用官方 TDesign 能力时直接注册并渲染官方组件；只有项目特有的业务组合才封装自定义组件。
- 修改前先阅读调用方、数据类型和相关依赖；保留用户已有改动，不顺手格式化无关文件。
- 提交保持单一职责，提交前先参照 `git log` 中的现有提交历史，沿用项目已经使用的类型、scope、语言和摘要风格；通常使用 `type(scope): 中文摘要`，常用类型为 `feat`、`fix`、`refactor`、`perf` 和 `chore`。
- 提交正文只记录关键改动和验证结果，多行正文使用真实换行，不写字面量 `\n`。

# 开发流程

1. 开始前检查 `git status --short --branch`，阅读相关 `AGENTS.md`、目标文件、调用方和依赖实现，确认改动边界。
2. 以最小可运行版本逐步开发；修改后检查组件注册、页面引用、事件和数据流，避免只完成局部代码。
3. 根据改动范围执行验证：变更 JavaScript 时运行 `node --check`，变更 JSON 时解析校验，统一运行 `git diff --check`。涉及页面或组件时，再用微信开发者工具验证真实渲染和交互。
4. 完成后复核 `git diff` 与 `git status`，确认只包含本次任务相关内容。除非用户明确授权，不提交、推送、打标签、删除或改写 Git 历史。

# Git 版本管理

版本按“大版本分支、小版本标签、最终合并发布”管理：

1. 从最新 `master` 创建对应的大版本开发分支，例如 `v1.0.x`、`v1.1.x` 或 `v2.1.x`。同一大版本的日常开发和修复都提交到该分支。
2. 每个提交保持单一职责，并参照目标版本分支已有提交历史确定提交风格、类型和 scope，不机械套用模板。确认某个提交可作为小版本发布时，在该提交上创建对应标签，例如 `v1.0.1`、`v1.1.1`。标签属于仓库级引用，但必须指向对应版本分支历史中的准确提交。
3. 创建或操作标签前核对提交、工作区和标签是否已存在；推送分支或标签、删除远端分支以及修改历史都必须得到明确授权。
4. 提交和小版本标签完成后默认停留在当前版本分支，不得主动切换到 `master`、合并、推送或发布。只有用户明确提出主分支发布需求后，才确认工作区和版本分支提交范围，并将 `vX.Y.x` squash 合入 `master`；主分支保留一个 `vX.Y.Z 中文摘要` 版本提交，版本分支继续保留完整开发历史。
5. 合并后核对提交对象、文件范围、工作区状态及两个分支最终内容：

   ```powershell
   git show --stat --oneline HEAD
   git status --short --branch
   git diff --exit-code master vX.Y.x
   ```

6. 发布完成后，不继续复用已经合并的旧版本分支；从最新 `master` 创建下一个 `vX.Y.x` 分支。若需要保留来源分支全部提交或明确记录合并边界，才使用既有的 `--ff-only` 或 `--no-ff` 方案。

## 提交交付流程

用户完成 review 并明确授权提交后，按以下顺序执行：

1. 提交前检查 `git status --short --branch`、当前分支和已有标签；确认未跟踪文件及其他无关改动，不把它们带入提交。
2. 按单一职责拆分提交。每个提交只暂存对应文件，使用 `git add -- <paths>`，随后检查 `git diff --cached --check` 和 `git diff --cached --name-status`，确认暂存范围准确。
3. 提交标题沿用目标分支历史，使用 `type(scope): 中文摘要` 格式，常用类型为 `feat`、`fix`、`refactor`、`perf` 和 `chore`，不要使用未经历史依据确认的版本号标题。
4. 提交正文使用真实换行：先写每个文件路径及对应变更描述，每条使用无序列表符号 `-`；结尾使用 `验证：`，逐条列出实际执行并通过的检查，不把计划中的验证写成已完成结果。
5. 每笔提交完成后立即记录提交哈希，并检查 `git show --stat --oneline <commit>`、`git status --short --branch`；下一笔提交只处理剩余的目标文件。
6. 用户明确要求创建小版本标签时，先确认标签不存在，再将标签指向指定的准确提交；通常版本标签应指向该版本最后一笔提交。创建后用 `git rev-list -n 1 <tag>` 和 `git show <tag>` 核对目标提交、标题和正文。
7. 所有提交和标签完成后停在当前版本分支，汇报提交哈希、标签目标、主分支未变更及未纳入的工作区文件；等待用户另行提出主分支发布请求。

## 合并示例

```powershell
git status --short --branch
git log --oneline master..v2.1.x
git switch master
git merge --squash v2.1.x
git diff --cached --check
git commit -m "v2.1.0 中文版本摘要"
git show --stat --oneline HEAD
git status --short --branch
git diff --exit-code master v2.1.x
git switch -c v2.2.x master
```

## 撤回合并

- 未推送且明确允许改写历史时，先确认合并前提交，再使用 `git reset --hard <合并前提交>`。
- 已推送的非快进合并优先使用 `git revert -m 1 <合并提交>`，不直接改写远端历史。
